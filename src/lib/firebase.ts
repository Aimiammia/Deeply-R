// Firebase integration has been removed as per user request for a local-only application.
// This file is kept to prevent build errors from any lingering imports, but it no longer contains
// active Firebase logic.

export const app = null;
export const auth = null;
export const db = null;
export const isFirebaseConfigured = false;
