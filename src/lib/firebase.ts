
// Firebase is not used in this version of the app.
// The app uses a local lock screen and localStorage for data persistence.
// This file is kept to prevent breaking the build process from any lingering imports.

export const isFirebaseConfigured = false;

export const app = null;
export const auth = null;
export const db = null;
