
'use client';

// This component is temporarily disabled.
// The local password lock screen has been removed to prepare the application
// for an upgrade to a full cloud-based authentication system (e.g., Firebase).
// This component will be replaced with a Login/Register screen in a future step.

export function LockScreen() {
  return null;
}
